apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.service.name }}
  labels:
    app: borathon-app
    component: {{ .Release.Name }}
spec:
  ports:
  - port: {{ .Values.service.port }}
    name: http-port
    targetPort: 8080
  type: {{ .Values.service.type }}
  selector:
    app: borathon-app
    component: {{ .Release.Name }}